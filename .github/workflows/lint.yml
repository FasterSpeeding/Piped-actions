name: Lint Actions

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  actionlint:
    uses: FasterSpeeding/Piped/.github/workflows/lint.yml@4e0ad805a4b43c13c760782f557fa21c24bfc13c
    secrets: inherit
